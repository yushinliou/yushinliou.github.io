---
import { Image } from 'astro:assets';

interface Props {
  images: {
    src: ImageMetadata;
    alt: string;
    caption: string;
  }[];
  gap?: string;
}

const { images, gap = "gap-1" } = Astro.props;
---

<div class={`grid grid-cols-1 md:grid-cols-2 ${gap} my-1 max-w-3xl mx-auto`}>
  {images.map((img) => (
    <figure class="flex flex-col">
      <div class="m-1 flex items-center justify-center">
        <Image 
          src={img.src} 
          alt={img.alt}
          quality={90}
          class="h-48 w-auto"
        />
      </div>
      <figcaption class="text-sm text-gray-600 dark:text-gray-400 text-center mt-0">
        {img.caption}
      </figcaption>
    </figure>
  ))}
</div>
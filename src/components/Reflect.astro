---
// src/components/Reflect.astro
interface Props {
  title?: string;
  icon?: string;
}

const { title = "Reflection", icon = "💭" } = Astro.props;
---

<!-- <div class="my-16 not-prose">
  <div class="max-w-3xl mx-auto bg-gradient-to-br from-rose-50 to-amber-50 dark:from-gray-800 dark:to-gray-800 rounded-2xl p-10 border border-rose-100 dark:border-gray-700">
    <div class="flex items-center gap-3 mb-6">
      <span class="text-3xl">{icon}</span>
      <h2 class="text-2xl font-medium text-gray-700 dark:text-gray-300 m-0">
        {title}
      </h2>
    </div>
    <div class="space-y-4 text-gray-600 dark:text-gray-300 [&>p]:m-0 [&>p:not(:last-child)]:mb-4 [&_strong]:text-gray-800 [&_strong]:dark:text-gray-100">
      <slot />
    </div>
  </div>
</div> -->


<div class="my-16 not-prose">

  
  <div class="max-w-3xl mx-auto bg-gray-50 dark:bg-gray-800 rounded-2xl p-12 border border-gray-200 dark:border-gray-700">
    <div class="flex items-center gap-3 mb-6">
      <span class="text-3xl">{icon}</span>
      <h2 class="text-2xl font-medium text-gray-700 dark:text-gray-300 m-0">
        {title}
      </h2>
    </div>

      <slot />
</div>